apiVersion: v1
kind: Service
metadata:
  name: spa-mobile-app
  namespace: {{ .Values.namespace }}
  labels:
    app: spa-mobile-app
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version }}
spec:
  type: ClusterIP
  selector:
    app: spa-mobile-app
  ports:
    - name: http
      port: 8080
      targetPort: 8080
