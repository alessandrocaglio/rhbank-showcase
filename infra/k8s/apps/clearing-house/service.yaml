apiVersion: v1
kind: Service
metadata:
  name: clearing-house
  namespace: showcase
spec:
  type: ClusterIP
  selector:
    app: clearing-house
  ports:
    - name: http
      port: 8080
      targetPort: 8080
