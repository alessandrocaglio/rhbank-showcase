apiVersion: v1
kind: Service
metadata:
  name: transaction-engine
  namespace: showcase
spec:
  type: ClusterIP
  selector:
    app: transaction-engine
  ports:
    - name: http
      port: 8080
      targetPort: 8080
