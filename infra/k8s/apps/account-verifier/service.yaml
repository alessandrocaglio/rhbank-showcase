apiVersion: v1
kind: Service
metadata:
  name: account-verifier
  namespace: showcase
spec:
  type: ClusterIP
  selector:
    app: account-verifier
  ports:
    - name: grpc
      port: 9090
      targetPort: 9090
    - name: http
      port: 8080
      targetPort: 8080
